# 衡标 · 员工考核系统

员工考核系统的本地可运行版本。当前业务范围与规则见 [业务需求说明](./BUSINESS-REQUIREMENTS.md)：员工用工号登录并填写树状指标；管理员配置模板、打分关系并审核；多名打分人独立评分；超级管理员管理组织、授权并查看全局评分明细。

## 已实现的业务能力

- 超级管理员管理企业、子公司、全员考核周期及管理员授权；组织按树状目录查看，敏感删除需输入确认语。
- 管理员按授权子公司维护员工信息（企业/子公司、部门、职位）、导入名单、创建和编辑树状指标模板，并批量匹配员工。
- 草稿周期内，员工可保存不完整的指标草稿；发布时统一校验指标树、人员和打分关系，发布后指标结构冻结。
- 管理员可对员工提交的指标树审核通过或逐项退回；退回意见会显示在员工的“我的指标”中，并通过右上角铃铛提醒。
- 一名员工可对应多名打分人。审核通过后，打分人独立评分且无需审核；当前已提交评分会锁定。管理员查看打分关系和完成进度，不查看评分细则。
- 全部打分完成后可导出最终总分；超级管理员可查看并导出跨企业评分明细。

## 技术与运行要求

- Node.js 22+、Next.js 16、React 19、PostgreSQL 16、Drizzle ORM、ExcelJS。
- 单台服务器可用 Docker Compose 运行应用和数据库；数据库数据保存在命名卷。
- 浏览器桌面/手机均可访问。`APP_ORIGIN` 必须设为浏览器实际访问的地址（协议、域名和端口）；正式环境使用 HTTPS 反向代理时，设为 `https://你的域名`，并将 `SESSION_COOKIE_SECURE=true`。

## 最快启动：Docker Compose

1. 复制 `.env.example` 为 `.env`，设置两个**不同的强密码**：`POSTGRES_PASSWORD` 与 `BOOTSTRAP_SUPER_ADMIN_PASSWORD`。本地开发时 `DATABASE_URL` 中的数据库密码也要与 `POSTGRES_PASSWORD` 一致；从 `localhost:3000` 访问时保留 `APP_ORIGIN=http://localhost:3000`。
2. 运行 `docker compose --profile setup run --rm --build setup`，完成数据库迁移与首次超级管理员初始化。`--build` 很重要：升级代码后它会确保 setup 容器包含新增迁移，而不是复用旧镜像。此命令可重复运行，不会覆盖已有超级管理员。
3. 运行 `docker compose up -d --build app`。
4. 打开 `http://localhost:3000`，使用 `.env` 中的超级管理员工号和密码登录。

如果本机没有 Docker，也可以自行准备 PostgreSQL 16，再执行 `npm ci`、`npm run db:migrate`、`npm run db:seed`、`npm run dev`。生产启动使用 `npm run build && npm run start`。

## 首次使用顺序

1. 超级管理员在“超级管理员”界面创建企业、子公司、考核周期。
   该界面也可查看、改名企业和子公司，并停用/启用子公司。子公司名称在全系统内不允许新增重名（忽略大小写及首尾空格）；升级前已有的重名记录不会自动合并，请在组织列表中逐条改名。
2. 在“管理员”界面新增员工，或下载模板后导入员工 Excel；初始密码至少 10 位，建议首次登录立即修改。
3. 超级管理员在“管理员授权”中给员工授予一个或多个子公司的管理权限；管理员同时保留员工身份。
4. 管理员在“指标模板”创建树状预设，在“人员配置”批量匹配员工并配置打分人；同一员工可配置多名打分人。员工与打分关系均可下载模板后批量导入。
5. 草稿周期内，员工仅保存可不完整的指标草稿；发布时系统统一校验每位在职员工都有考核单、打分人以及完整的 100 分指标树。未通过校验不能发布。
6. 周期发布后指标结构冻结。员工提交管理员预审，管理员通过或逐项给意见退回；审核通过后才生成打分任务。所有打分人提交后计算平均总分；管理员仅能看到打分关系、完成进度和总分导出入口，不能查看打分细项；超级管理员可查看并导出明细。

## 校验与测试

```sh
npm run typecheck
npm test
npm run lint
npm run build
```

## 已知限制与生产前注意事项

- 退回提醒目前通过页面右上角铃铛展示；“催办”不发送短信、邮件或企业微信等外部消息。
- Excel 员工与打分关系导入先预校验、再确认，默认整份文件原子写入；当前限制为单次 500 行、2 MB。初始密码存放于员工导入文件，请在导入后安全删除该文件。
- 当前最终得分是所有打分人 100 分制总分的等权平均，不支持评分人权重。
- 当前指标编辑界面支持一级、二级树状结构；尚不支持更深层级、附件、PWA 离线草稿、周期自动归档、评分人权重和外部消息通知。
- 部署到公网前应配置 HTTPS、备份 PostgreSQL 数据卷、限制管理员账号分发，并增加登录限速及操作审计查看页面。当前版本适合本地试用/内网验收，不建议未经加固直接公开到互联网。

## 文档

- [当前业务需求说明](./BUSINESS-REQUIREMENTS.md)：根据实际代码反推的角色、流程、规则、限制与验收标准。
