# 多人打分加权平均任务清单

## Task 1：权重存储与领域规则

- [x] 新增迁移、Drizzle schema、默认均分与权重校验函数。
- 验收：单人 100；三人默认 `33.34/33.33/33.33`；99.99、100.01 与零权重均被拒绝。
- 验证：`npm test -- src/server/domain.test.ts`、`npm run typecheck`。
- 依赖：无。

## Task 2：配置与评分工作流

- [x] 配置/导入打分关系时原子保存权重；发布校验权重；审核通过时写入评分任务快照；按快照计算最终分。
- 验收：草稿期可改、发布后不可改、最终分按权重计算。
- 验证：`npm test -- src/app/api/v1/organization.test.ts`、`npm test`。
- 依赖：Task 1。

## Task 3：查询与 Excel 契约

- [x] 状态接口返回权重；打分关系模板/导入增加权重列；评分明细导出增加权重、总分、加权贡献。
- 验收：页面、导入和导出的权重一致。
- 验证：API 测试与人工导入/导出检查。
- 依赖：Task 2。

## Task 4：管理员权重配置界面

- [x] 在人员配置的打分关系区域展示可编辑权重、合计与校验提示。
- 验收：选择多人后默认均分，保存前必须显示 `100.00 / 100.00`。
- 验证：浏览器检查、`npm run lint`、`npm run build`。
- 依赖：Task 3。

## Checkpoint：完成

- [x] `npm run typecheck`、`npm test`、`npm run lint`、Docker 生产构建全部通过。
- [x] 执行迁移并重建本地 Docker 应用。
- [x] 用单人/多人权重的领域测试、数据库回填检查和状态接口冒烟检查验证数据链路。
